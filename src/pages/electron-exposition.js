import Layout from '../components/Layout'
import React from 'react'

const ElectronExpositionPage = ({ location }) => {
    return(
        <Layout location={location}>
         <h2>Electron Exposition</h2>
        </Layout>
    )
}

export default ElectronExpositionPage